{
  "name": "multimap-test",
  "version": "1.0.0",
  "description": "",
  "main": "bar.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
      "babel": "node_modules/.bin/babel *.js --presets es2015 --source-maps inline -d build/",
      "babel-external": "node_modules/.bin/babel *.js --presets es2015 --source-maps -d build/",
      "concat": "mkdir -p target/ && for i in build/*.js ; do cat $i ; echo ''; done > target/out.js",
      "concat-external": "mkdir -p target/ && for i in build/*.js ; do cat $i ; echo ''; done > target/out.js && cp build/*.map target/",
      "clean": "rm -rf target/ build/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel": "^6.3.26",
    "babel-cli": "^6.4.5",
    "babel-preset-es2015": "^6.3.13"
  }
}
